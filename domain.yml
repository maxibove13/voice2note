version: "3.1"

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
  
intents:
- user_message

actions:
- action_under_construction

responses:
  utter_action_under_construction:
  - text: "This action is under construction"
